// Deprecated — use <concurrency/task_system.hpp> instead.
// This header exists for backward compatibility and re-exports
// the library types into the global namespace (not recommended).
#include <concurrency/task_system.hpp>
using concurrency::spin_mutex;
using concurrency::ticket_mutex;
using concurrency::task_system;
